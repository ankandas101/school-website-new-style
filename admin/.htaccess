# Prevent directory listing
Options -Indexes

# (Optional) Prevent PHP execution in admin folder
# <FilesMatch "\.(php|php5|phtml)$">
#     Deny from all
# </FilesMatch> 

RewriteEngine On

RewriteRule ^$ login.php [R=301,L]